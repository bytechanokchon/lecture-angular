# Pipe
เป็นเครื่องมือที่่ใช้ในการจัดการข้อมูลก่อนการแสดงผล โดยจะทำงานใน template เท่านั้น 

**โครงสร้างการใช้งาน**

    {{ value | pipeName: arguments }}

**ตัวอย่าง**

แปรงรูปแบบของวันที่ก่อนการแสเงผล

    {{ task.dueDate | date:'short' }}